package com.example.demo;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class BookApplication {

	public static void main(String[] args) {
		SpringApplication.run(BookApplication.class, args);
	}


//	Author : name, age, gender, rating, hashMap<String, Book> Book{ Int pages; Author name; }

//	removeAdmin(User approver, User user, Group group):
//	Create an author and book management system
//	Hashmap (book, attributes pages, name , authorname)
}
